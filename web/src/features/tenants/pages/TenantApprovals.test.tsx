import{cleanup,render,screen}from'@testing-library/react';import{afterEach,beforeEach,describe,expect,it,vi}from'vitest';import{TenantApprovals}from'./TenantApprovals';
const tenant={public_id:'01TENANT',institution_name:'Nalanda Academy',legal_name:'Nalanda Pvt Ltd',requested_slug:'nalanda',constitution_type:'private_limited_company',home_region:'ap-south-1',country_code:'IN',status:'submitted',submitted_at:'2026-08-10',applicant:{display_name:'Asha Rao',email_masked:'a***@test.com',phone_masked:'+91 ***1234'},tax_identifiers:[]};
describe('TenantApprovals',()=>{beforeEach(()=>vi.stubGlobal('fetch',vi.fn().mockResolvedValue({ok:true,json:async()=>({data:[tenant],meta:{current_page:1,last_page:1,total:1}})})));afterEach(()=>{cleanup();vi.unstubAllGlobals()});it('provides list search edit delete and approval controls',async()=>{render(<TenantApprovals/>);expect(await screen.findByText('Nalanda Academy')).toBeInTheDocument();expect(screen.getByLabelText('Search tenants')).toBeInTheDocument();expect(screen.getByRole('button',{name:'Edit'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'Delete'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'Approve & assign admin'})).toBeInTheDocument()})});
