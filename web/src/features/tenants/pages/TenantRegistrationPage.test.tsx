import{cleanup,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it}from'vitest';import{TenantRegistrationPage}from'./TenantRegistrationPage';
describe('TenantRegistrationPage',()=>{afterEach(cleanup);it('collects institution, administrator and tax details',()=>{render(<TenantRegistrationPage/>);expect(screen.getByRole('heading',{name:'Register your institution'})).toBeInTheDocument();expect(screen.getByLabelText('Institution name')).toBeRequired();expect(screen.getByLabelText('Email')).toBeRequired();expect(screen.getByLabelText('Tax number')).toBeRequired();});});
