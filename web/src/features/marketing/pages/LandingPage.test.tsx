import{cleanup,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it}from'vitest';import{LandingPage}from'./LandingPage';
describe('LandingPage',()=>{afterEach(cleanup);it('presents LMS features and authentication actions',()=>{render(<LandingPage/>);expect(screen.getByRole('heading',{name:/Teach brilliantly/i})).toBeInTheDocument();expect(screen.getAllByRole('link',{name:/sign up/i}).length).toBeGreaterThan(0);expect(screen.getAllByRole('link',{name:/sign in/i}).length).toBeGreaterThan(0);expect(screen.getByText('Live classrooms')).toBeInTheDocument();expect(screen.getByText('Teacher-verified AI')).toBeInTheDocument();expect(screen.getByText('Online and offline')).toBeInTheDocument()})});
