import{cleanup,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it}from'vitest';import{AuthPage}from'./AuthPages';
describe('authentication pages',()=>{afterEach(cleanup);it('renders sign-up verification fields',()=>{render(<AuthPage mode="signup"/>);expect(screen.getByRole('heading',{name:'Create your account'})).toBeInTheDocument();expect(screen.getByLabelText('Mobile number')).toBeRequired()});it('renders password reset token',()=>{render(<AuthPage mode="reset"/>);expect(screen.getByLabelText('Reset token')).toBeRequired();expect(screen.getByLabelText('Confirm password')).toBeRequired()})});
